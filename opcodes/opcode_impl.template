#include <@OPCODE@.h>
#include <vm.h>

#include <iostream>

namespace primal
{

    bool impl_@OPCODE@(vm* v)
    {
        options::instance().asm_stream() << "@OPCODE@" << std::endl;
        return false;
    }

}