#include <AND.h>
#include <token.h>

#include <iostream>
#include <vector>

std::vector<uint8_t> compile_AND(std::vector<token>&)
{
    std::cout << "compile: AND" << std::endl;
    return std::vector<uint8_t>();
}

